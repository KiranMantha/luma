# API server port
PORT=3002

# SQLite DB file path (relative to apps/api)
DB_FILE=./cms.db

# CORS origins for API (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Directory where published page JSON models are written (optional)
PUBLIC_PAGES_DIR=public/pages